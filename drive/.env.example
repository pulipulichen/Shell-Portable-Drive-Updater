# rclone remote 名稱（只填名稱，不含冒號），例如：gdrive
RCLONE_REMOTE=gdrive

# remote 底下要同步的子資料夾路徑，例如：Tools/iso
RCLONE_REMOTE_SUBDIR=Tools/iso

# 同步到本機（可攜硬碟）的目錄，可用相對或絕對路徑
LOCAL_SYNC_DIR=.

# 同步時要排除的資料夾名稱前綴（可留空），例如：[ 會排除 [開頭的目錄
EXCLUDE_PREFIX=[

